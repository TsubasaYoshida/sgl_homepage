<% if notice %>
  <div class="alert alert-success my-4" role="alert"><strong>完了</strong> - <%= notice %></div>
<% end %>

<h2 class="float-left">大会日程管理</h2>

<div class="padding-bottom10 padding-top10 float-right">
  <%= link_to '新規作成', new_event_info_path, class: 'btn btn-default' %>
</div>

<table class="table table-bordered text-center">
  <tr class="bg-green">
    <th>年度</th>
    <th>春季／秋季</th>
    <th>大会名</th>
    <th>最終更新日</th>
    <th colspan="3"></th>
  </tr>
  <% @event_infos.each do |event_info| %>
    <tr>
      <td style="vertical-align: middle;">
        <%= event_info.year %>
      </td>
      <td style="vertical-align: middle;">
        <%= event_info.season %>
      </td>
      <td style="vertical-align: middle;">
        <%= event_info.league %>
      </td>
      <td style="vertical-align: middle;">
        <%= l event_info.updated_at, format: :standard %>
      </td>
      <td>
        <%= link_to '大会情報編集', edit_event_info_path(event_info), class: 'btn btn-default' %>
      </td>
      <td>
        <%= link_to '日程情報編集', one_days_management_event_info_path(event_info), class: 'btn btn-default' %>
      </td>
      <td>
        <%= link_to '大会ごと削除', event_info, method: :delete, data: {confirm: '削除すると順位表ページが表示できなくなりますが、削除してよろしいですか？'}, class: 'btn btn-default' %>
      </td>
    </tr>
  <% end %>
</table>

<div class="alert alert-warning my-4" role="alert">
  <h5 class="alert-heading">使用上の注意</h5>
  <p class="mb-0">
    日程情報は順位表ページの表示と関連しています。<br>
    そのため、大会終了後も日程情報は削除しないでください。<br>
    （誤入力などで誤ったデータを作成してしまった場合は削除をお願いします）
  </p>
</div>
